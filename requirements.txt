# REDACTS - Python runtime dependencies.
# Mirrors PYTHON_PACKAGES in static/cli/dependencies.py and the
# [project.dependencies] table in pyproject.toml. Edit all three in
# lock-step. Install: pip install -r requirements.txt

chardet>=5.0.0
magika>=0.6.0
paramiko>=3.4.0
requests>=2.31.0
py7zr>=1.1.3
rarfile>=4.1
rich>=13.0.0
pyyaml>=6.0
tree-sitter>=0.23.0
tree-sitter-php>=0.23.0
stix2>=3.0.0
semgrep>=1.0.0
playwright>=1.51.0
pytest-asyncio>=0.24.0
keyring>=25.0.0
